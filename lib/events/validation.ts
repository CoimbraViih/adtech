/**
 * Shared validation constants for event explorer and export routes.
 * Ensures consistency across all date validation logic.
 */

export const ISO_DATE = /^\d{4}-\d{2}-\d{2}$/
export const MAX_RANGE_DAYS = 90
